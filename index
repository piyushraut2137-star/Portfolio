<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Piyush Raut — Portfolio</title>

  <style>
    :root{
      --bg:#fafafa; --card:#ffffff; --accent:#0f62fe; --muted:#666;
      --maxw:1100px;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background:var(--bg);
      color:#111;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      line-height:1.5;
    }

    /* Header */
    header{
      background:linear-gradient(90deg,#08357a 0%, #0f62fe 100%);
      color:#fff;
      padding:2.25rem 1rem;
      text-align:center;
    }
    header .container{
      max-width:var(--maxw);
      margin:0 auto;
    }
    header h1{ margin:0; font-size:clamp(1.6rem,2.6vw,2.6rem); }
    header p{ margin:0.3rem 0 0; color:rgba(255,255,255,0.9) }

    /* Layout container */
    .container{
      max-width:var(--maxw);
      margin:2rem auto;
      padding:0 1rem;
    }

    /* Sections */
    section{ background:transparent; margin-bottom:1.5rem; }
    section h2{
      font-size:1.25rem;
      margin:0 0 0.75rem;
      display:inline-block;
      border-bottom:3px solid var(--accent);
      padding-bottom:0.35rem;
    }

    /* About */
    .about{
      display:grid;
      grid-template-columns: 160px 1fr;
      gap:1.25rem;
      align-items:center;
    }
    .about img{
      width:160px;
      height:160px;
      object-fit:cover;
      border-radius:12px;
      border:4px solid #fff;
      box-shadow:0 6px 18px rgba(5,10,30,0.12);
      background:linear-gradient(180deg,#eee,#ddd);
    }
    .about p{ margin:0.25rem 0 0; color:var(--muted) }

    /* Skills */
    .skills-list{
      display:flex;
      flex-wrap:wrap;
      gap:.5rem;
      margin-top:.5rem;
    }
    .skill{
      background:var(--card);
      border:1px solid #eee;
      padding:.45rem .65rem;
      border-radius:8px;
      font-size:.95rem;
      box-shadow:0 4px 10px rgba(12,18,40,0.04);
    }

    /* Projects grid */
    .projects-grid{
      display:grid;
      grid-template-columns:repeat(2,1fr);
      gap:1rem;
      margin-top:1rem;
    }
    .project{
      background:var(--card);
      border-radius:10px;
      overflow:hidden;
      border:1px solid #eee;
      transition:transform .22s ease, box-shadow .22s ease;
    }
    .project:hover{ transform:translateY(-6px); box-shadow:0 14px 30px rgba(10,20,60,0.09) }
    .project img{ width:100%; height:160px; object-fit:cover; display:block; }
    .project .pcontent{ padding:0.9rem; }
    .project h3{ margin:0 0 .25rem; font-size:1.05rem }
    .project p{ margin:0; color:var(--muted); font-size:.95rem }

    /* Resume */
    .resume a{
      display:inline-block;
      padding:.6rem 1rem;
      background:var(--accent);
      color:white;
      border-radius:8px;
      text-decoration:none;
      font-weight:600;
      box-shadow:0 8px 18px rgba(15,98,254,0.18);
    }
    .resume a:active{ transform:translateY(1px) }

    /* Contact */
    .contact p{ margin:.25rem 0; color:var(--muted) }
    .contact .contact-line{ display:flex; gap:.5rem; align-items:center; flex-wrap:wrap }

    /* Footer */
    footer{
      text-align:center;
      padding:1.25rem;
      color:#fff;
      background:#071638;
      margin-top:1.5rem;
    }

    /* Responsive */
    @media (max-width:820px){
      .about{ grid-template-columns: 120px 1fr; gap:.75rem; }
      .projects-grid{ grid-template-columns: 1fr; }
      .about img{ width:120px; height:120px; }
      header{ padding:1.5rem 1rem; }
      .container{ margin:1rem auto; }
    }

    /* Small helpers */
    .muted{ color:var(--muted); font-size:.95rem }
    nav.topnav{ margin-top:.6rem }
    nav.topnav a{
      color:rgba(255,255,255,0.9);
      margin:0 .5rem;
      text-decoration:none;
      font-weight:600;
      font-size:.95rem;
    }
    nav.topnav a:hover{ text-decoration:underline }
  </style>
</head>
<body>

  <!-- Header -->
  <header>
    <div class="container">
      <h1>Piyush Raut</h1>
      <p>Frontend Developer • UI Enthusiast • Problem Solver</p>
      <nav class="topnav" aria-label="Main navigation">
        <a href="#about">About</a> ·
        <a href="#skills">Skills</a> ·
        <a href="#projects">Projects</a> ·
        <a href="#resume">Resume</a> ·
        <a href="#contact">Contact</a>
      </nav>
    </div>
  </header>

  <main>

    <div class="container">

      <!-- About -->
      <section id="about" aria-labelledby="about-heading">
        <h2 id="about-heading">About</h2>
        <div class="about" style="margin-top:.75rem;">
          <!-- Replace profile.jpg with your picture -->
          <img src="img1.jpg" alt="Piyush Raut — profile photo">
          <div>
            <h3 style="margin:0;">Hi — I'm Piyush Raut</h3>
            <p class="muted">
              I build clean, responsive websites and web apps. I enjoy turning ideas into accessible,
              user-friendly interfaces. I'm comfortable with modern HTML, CSS, and JavaScript and love learning new tools.
            </p>
            <p style="margin-top:.6rem;">
              <strong>Location:</strong> Pune City, Country ·
              <strong>Available for:</strong> Freelance / Full-time
            </p>
          </div>
        </div>
      </section>

      <!-- Skills -->
      <section id="skills" aria-labelledby="skills-heading">
        <h2 id="skills-heading">Skills</h2>
        <div class="skills-list" style="margin-top:.65rem;">
          <div class="skill">HTML5</div>
          <div class="skill">CSS3 (Flexbox & Grid)</div>
          <div class="skill">Responsive Design</div>
          <div class="skill">JavaScript (ES6+)</div>
          <div class="skill">Git & GitHub</div>
          <div class="skill">Accessibility (a11y)</div>
        </div>
      </section>

      <!-- Projects -->
      <section id="projects" aria-labelledby="projects-heading">
        <h2 id="projects-heading">Projects</h2>

        <div class="projects-grid">
          <!-- Project 1 -->
          <article class="project" aria-labelledby="proj1-title">
            <!-- replace project1.jpg -->
            <img src="project1.jpg" alt="Project 1 screenshot">
            <div class="pcontent">
              <h3 id="proj1-title">Personal Portfolio</h3>
              <p>A responsive portfolio site built with semantic HTML and CSS Grid. Includes project showcase and contact form.</p>
            </div>
          </article>

          <!-- Project 2 -->
          <article class="project" aria-labelledby="proj2-title">
            <!-- replace project2.jpg -->
            <img src="project2.jpg" alt="Project 2 screenshot">
            <div class="pcontent">
              <h3 id="proj2-title">To-Do App</h3>
              <p>A minimal to-do application demonstrating DOM manipulation, localStorage persistence, and responsive layout.</p>
            </div>
          </article>
        </div>
      </section>

      <!-- Resume -->
      <section id="resume" aria-labelledby="resume-heading">
        <h2 id="resume-heading">Resume</h2>
        <p style="margin-top:.6rem;" class="muted">Download a PDF copy of my resume for full work history and education.</p>
        <!-- Replace resume.pdf with your resume file -->
        <div class="resume" style="margin-top:.6rem;">
          <a href="resume.pdf" download>Download Resume (PDF)</a>
        </div>
      </section>

      <!-- Contact -->
      <section id="contact" aria-labelledby="contact-heading">
        <h2 id="contact-heading">Contact</h2>
        <div class="contact" style="margin-top:.6rem;">
          <p class="muted">Feel free to reach out for work, collaboration, or a friendly chat.</p>
          <div class="contact-line" style="margin-top:.5rem;">
            <p style="margin:0"><strong>Email:</strong> <a href="mailto:piyushraut2137@gmail.com">piyushraut2137@gmail.com</a></p>
            <p style="margin:0"><strong>Phone:</strong> +91 9175566088</p>
          </div>
          <p style="margin-top:.6rem; font-size:.95rem" class="muted">
            You can also find me on <a href="#" rel="noopener">GitHub</a> · <a href="#" rel="noopener">https://www.linkedin.com/in/piyushraut-a5a154322
</a>
          </p>
        </div>
      </section>

    </div>
  </main>

  <!-- Footer -->
  <footer>
    <div class="container">
      <small>&copy; 2025 Piyush Raut — All Rights Reserved</small>
    </div>
  </footer>

</body>
</html>
